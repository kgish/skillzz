<%= simple_form_for([category, skill]) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :tag_names, label: "Tags" %>

  <%= f.button :submit, class: "btn-primary" %>
<% end %>