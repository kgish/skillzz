<% title(@skill.name, 'Skills') %>

<h1><%= @skill.name %></h1>

<%= link_to "Edit Skill", edit_skill_path(@skill) %>
<%= link_to "Delete Skill", skill_path(@skill), method: :delete,
            data: { confirm: "Are you sure you want to delete this skill?" } %>
